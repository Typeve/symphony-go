# Implementation Plans

Generated by the improve skill on 2026-06-17. Execute in the order below unless
dependencies say otherwise. Each executor must read the plan fully before
starting, honor its STOP conditions, and update the status row when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Harden MVP execution pipeline | P1 | M | - | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- 001 should land before any feature expansion. It protects the core Gitea issue -> Codex -> reviewer -> execution branch path from committing sensitive agent artifacts, failing silently, or drifting from the documented MVP contract.

## Findings Considered And Rejected

- Implement full External Validator / Verdict File / Publish Gate in this pass: rejected for this plan because `AGENTS.md` and `README.md` define the current target as a simplified MVP using Codex plus a reviewer command, not the older validator workflow. This plan aligns stale docs to the simplified contract instead of expanding the runtime.
- Reintroduce HTTP status API: rejected because `AGENTS.md` explicitly says to delete `httpapi` and keep the MVP small.
- Add Linear or multi-client tracker abstractions: rejected because `AGENTS.md` explicitly says only Gitea tracker remains in scope.
